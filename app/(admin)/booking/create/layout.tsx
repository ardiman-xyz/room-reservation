import React, { ReactNode } from "react";

const BookingCreateLayout = ({ children }: { children: ReactNode }) => {
  return <div className=" container mx-auto max-w-4xl">{children}</div>;
};

export default BookingCreateLayout;
